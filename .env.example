# В dev — адрес express-lms. По умолчанию Express слушает :3000.
# Next.js dev-сервер этого проекта запускается на :3001 (см. package.json), чтобы порты
# не конфликтовали — не забудь синхронизировать FRONTEND_URL в express-lms/.env с этим портом.
NEXT_PUBLIC_API_URL=http://localhost:3000
